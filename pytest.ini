[pytest]
minversion = 7.0
testpaths = tests
addopts = --cov=src --cov-report=term-missing --cov-report=xml --strict-markers --strict-config --maxfail=1
asyncio_mode = auto
filterwarnings =
    ignore::telegram.warnings.PTBUserWarning
